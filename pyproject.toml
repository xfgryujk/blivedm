[tool.poetry]
name = "blivedm"
version = "0.1.1"
description = "Python获取bilibili直播弹幕的库，使用WebSocket协议"
authors = ["xfgryujk"]
readme = "README.md"
packages = [{include = "blivedm"}]

[tool.poetry.dependencies]
python = "^3.8"
aiohttp = ">=3.7.4"
brotli = "1.0.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
